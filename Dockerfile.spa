FROM nginx:alpine
COPY . /usr/share/nginx/html
RUN echo 'server { listen 80; root /usr/share/nginx/html; index spa.html; location / { try_files $uri $uri/ /spa.html; } }' > /etc/nginx/conf.d/default.conf
EXPOSE 80
